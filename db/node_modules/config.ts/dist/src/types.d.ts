export declare type TString = 'string';
export declare type TNumber = 'number';
export declare type TBoolean = 'boolean';
export declare type TConfigVariable = TString | TNumber | TBoolean;
export interface ConfigurationStructure {
    [key: string]: TConfigVariable;
}
export declare type ConfigurationOutput<TStructure> = {
    [k in keyof TStructure]: TStructure[k] extends TString ? string : TStructure[k] extends TNumber ? number : TStructure[k] extends TBoolean ? boolean : null;
};
