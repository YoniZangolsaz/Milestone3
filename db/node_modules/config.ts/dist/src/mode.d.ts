export declare type RunMode = 'LOADER' | 'CONFIGURATOR';
export declare class RunModeConfiguration {
    private runMode;
    get(): RunMode;
    set(runMode: RunMode): void;
}
export declare const runMode: RunModeConfiguration;
